# 測試驗收規格

## 1. 文件資訊

| 項目 | 內容 |
| --- | --- |
| 文件名稱 | 測試驗收規格 |
| 文件代碼 | Health-and-Injury-Co-Insurance-System_TEST_測試驗收 |
| 目前版本 | **v1.5** |
| 建立日期 | 2026-08-03 |
| 最後更新 | 2026-08-14 |
| 作者 | AI 分析 |
| 狀態 | Draft |

> **文件維護原則**：單一常駐文件，改版直接更新本檔，版本歷程見 §8。

**依據**：`..._REQ_需求規格.md`（v1.0）、`..._DESIGN_系統設計.md`（v1.0）、`..._RULE_規則定義.md`（v3.2）、`..._TASK_任務拆分.md`（v1.0）。

---

## 2. 背景與目的

定義可執行、可自動化之測試案例，作為開發完成之驗收依據。

### 2.1 測試資料基準

| 項目 | 來源 |
| --- | --- |
| 標準輸入 | `docs/規格來源/第一階段-共保月帳單/檔案位子範例/`（config / input / templet 皆已同步，可直接複製為測試資源） |
| 保費檔 | `VOLP11505.csv`，2,193 資料列，全為 N05 |
| 理賠檔 | `VOLC11505.csv`，14 資料列，簽單年度 113/114/115 |
| 設定檔 | `config/application.xlsx`，16 家、合計 100% |

### 2.2 驗收數值基準

| 項目 | 基準值 |
| --- | --- |
| 共保保費（M1） | **350,123** |
| 攤付共保賠款（M3，簽單年度 = 115） | **126,931** |
| 非中央再保 15 家分攤保費合計 | **325,611** |
| 中央再保分攤保費（M5[N19]） | **24,512** |
| 共保管理費（M7，費率 **6%**） | **21,009** |
| Balance Due（M8） | **202,183** |
| 中央再保應攤配賠款 | **+8,882**（2026-08-10 起為正值） |
| 中央再保應繳共保管理會費 | **+1,471** |
| 富邦產險應繳共保管理會費 | **+1,260** |

**第二階段（賠款 T 字帳）基準值**：

| 項目 | 基準值 |
| --- | --- |
| 簽單年度分群（M9） | `{115: 126931, 114: 38331, 113: 32460}`，Σ = **197,722** |
| 應產出年度（M10） | `[114, 113]`（排除 115、降冪） |
| 113 年報表之四格金額 | **32,460**（`G6` = `G21` = `O20` = `O21`），`P4` = `2024` |
| 114 年報表之四格金額 | **38,331**，`P4` = `2025` |
| 產出檔數 | **2 + 2 = 4** 檔 |
| 跨階段不變式 | `M9[115]` = M3 = 126,931；`32,460 + 38,331 + 126,931 = 197,722` |

> **第二階段之禁止事項**：**不得以第一階段之 M8 算式驗證賠款 T 字帳之 Balance Due**（會得 **−38,331**，符號相反）；
> **不得以 `docs/規格來源/第二階段-賠款月帳單/產出範例/` 之版面作為基準**（`I3`+`J3` 兩格、`O4` 含年、`O6`/`G14` 為字串，皆為舊版；**僅金額 32,460 / 38,331 可信**）。

> **禁止事項**：**不得以 `docs/規格來源/第一階段-共保月帳單/產出範例/` 之金額作為比對基準**。該兩份範例為舊成分計算結果，管理費、Balance Due、F22、G22 皆已不同（R-06）。

### 2.3 測試分類與編號

| 前綴 | 類別 | 自動化 |
| --- | --- | --- |
| `TC-U-xx` | 單元測試 | 可 |
| `TC-E2E-xx` | 端到端整合測試 | 可 |
| `TC-N-xx` | 反向／例外測試 | 可 |
| `TC-M-xx` | 手動驗收 | 否 |

---

## 3. 單元測試

### TC-U-01 欄位索引與表頭常數

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 確認 `PremiumColumn` / `ClaimColumn` 之索引與預期表頭與 MAPPING 完全一致 |
| 測試資料 | MAPPING §3／§4 之欄位清單 |
| 執行步驟 | 1. 取得兩個 enum 之全部常數<br>2. 逐項比對索引與表頭字串 |
| 預期結果 | `PremiumColumn` 19 項、`ClaimColumn` 22 項；`ClaimColumn.UNDERWRITING_YEAR` 索引為 **5**；`PremiumColumn.TOTAL_PREMIUM` 索引為 **17**；`ClaimColumn.TOTAL_SETTLED_CLAIM` 索引為 **21** |
| 驗收標準 | 全部相符 |
| 自動化 | 可 |

### TC-U-02 設定檔讀取

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證動態讀取、對照建立與衍生欄位 |
| 測試資料 | `config/application.xlsx` |
| 執行步驟 | 呼叫 `SettingReader.load()` |
| 預期結果 | 16 家；首筆 台灣人壽/L02/**0.075**、末筆 國泰產險/N15/0.06、中央再保 N19/**0.07**；成分合計 = 1.00；`rocYearMonth()` = `11505`；`adYear()` = 2026 |
| 驗收標準 | 全部相符 |
| 自動化 | 可 |

### TC-U-03 捨入模式

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 確認全案採 HALF_UP 而非 HALF_EVEN |
| 測試資料 | 0.5、1.5、2.5、−0.5、17506.15、10503.69 |
| 執行步驟 | 呼叫 `RoundingUtil.round()` |
| 預期結果 | 1、2、**3**（HALF_EVEN 會得 2）、−1、17506、10504 |
| 驗收標準 | 全部相符 |
| 自動化 | 可 |

### TC-U-04 共保保費彙總

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證含負值、不去重之全檔加總 |
| 測試資料 | `VOLP11505.csv`（含 100 列負值） |
| 執行步驟 | 呼叫 `PremiumCalculator.total()` 與 `byCompany()` |
| 預期結果 | 總額 = **350,123**；`byCompany` 僅 N05 一鍵，值 350,123 |
| 驗收標準 | 相符；且以「過濾負值」之實作會得不同值（可作為反向確認） |
| 自動化 | 可 |

### TC-U-05 攤付共保賠款（簽單年度篩選）

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證僅篩年度、不篩月份 |
| 測試資料 | `VOLC11505.csv`（簽單年度 113/114/115），設定年 115 |
| 執行步驟 | 呼叫 `ClaimCalculator.total()` |
| 預期結果 | **126,931** |
| 驗收標準 | 相符；**若得 197,722 表示未篩選，測試必須失敗** |
| 自動化 | 可 |

### TC-U-06 應分配保費與差額法

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證非中央再保之乘算與中央再保之差額法 |
| 測試資料 | M1 = 350,123；16 家成分 |
| 執行步驟 | 呼叫 `AllocationCalculator.allocatePremium()` |
| 預期結果 | 非 N19 合計 = **325,611**；N19 = **24,512**（≠ 350,123 × 7% = 24,509）；報表值 F[N19] = −24,512；F 欄合計 = −350,123 |
| 驗收標準 | 全部相符 |
| 自動化 | 可 |

### TC-U-07 應攤配賠款

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 同 TC-U-06，基礎改為賠款總額 |
| 測試資料 | M3 = 126,931 |
| 執行步驟 | 呼叫 `AllocationCalculator.allocateClaim()` |
| 預期結果 | N19 = **+8,882**；**G 欄合計 = +126,931**（2026-08-10 起 G 欄為正值） |
| 驗收標準 | 相符 |
| 自動化 | 可 |

### TC-U-08 共保管理費與捨入順序

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證「逐家四捨五入 → ×**6%** → 再四捨五入 → 加總」之順序 |
| 測試資料 | M1 = 350,123；16 家成分 |
| 執行步驟 | 呼叫 `ManagementFeeCalculator.total()` |
| 預期結果 | **21,009**；M6[N05] = **1,260**；M6[N19] = **1,471** |
| 驗收標準 | 相符；以「先加總再乘 6%」之實作會得不同值 |
| 自動化 | 可 |

### TC-U-09 Balance Due（含負值）

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證正常值與負值處理 |
| 測試資料 | (a) M1=350,123 / M3=126,931 / M7=**21,009**<br>(b) 人工構造：M1=100,000 / M3=500,000 / M7=5,000 |
| 執行步驟 | 呼叫 `calculateBalanceDue()` |
| 預期結果 | (a) **202,183**；(b) **−405,000**（照實填入負數，**非空字串、非 0**） |
| 驗收標準 | 相符 |
| 自動化 | 可 |

### TC-U-10 中央再保依代號判定

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 確認不依列號判定 |
| 測試資料 | 兩份設定檔：N19 分別位於第 9 列與最末列，其餘相同 |
| 執行步驟 | 分別執行計算 |
| 預期結果 | 兩者之 M5/M6/M7/M8 完全相同 |
| 驗收標準 | 相符 |
| 自動化 | 可 |

### TC-U-11 民國年與日期工具

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證年份換算與 `yyymmdd` 檢核 |
| 測試資料 | 115；`1150604`（合法）、`671116`（6 碼）、`1151332`（月份 13） |
| 執行步驟 | 呼叫 `RocDateUtil` |
| 預期結果 | 115 → 2026；`1150604` 通過；`671116` 與 `1151332` 不通過 |
| 驗收標準 | 相符 |
| 自動化 | 可 |

### TC-U-12 個資遮蔽

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證遮蔽格式 |
| 測試資料 | `A123456789`、`王小明`、`0671116` |
| 執行步驟 | 呼叫 `MaskUtil` |
| 預期結果 | `A12****789`、`王＊＊`、`067****` |
| 驗收標準 | 相符 |
| 自動化 | 可 |

### TC-U-13 保費檔欄位檢核（正向）

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 現行樣本應全數通過 |
| 測試資料 | `VOLP11505.csv` 2,193 列 |
| 執行步驟 | 呼叫 `PremiumValidator.validate()` |
| 預期結果 | 錯誤數 = **0**（含 2,072 列批單起迄日空白之情形） |
| 驗收標準 | 相符 |
| 自動化 | 可 |

### TC-U-14 理賠檔欄位檢核（正向）

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 現行樣本應全數通過 |
| 測試資料 | `VOLC11505.csv` 14 列（出生日期 7 碼） |
| 執行步驟 | 呼叫 `ClaimValidator.validate()` |
| 預期結果 | 錯誤數 = **0** |
| 驗收標準 | 相符 |
| 自動化 | 可 |

### TC-U-15 簽單年度分群（M9）

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證 R-CALC-17：依簽單年度分群、**全年度皆計不篩選** |
| 測試資料 | `VOLC11505.csv` 14 列 |
| 執行步驟 | 呼叫 `ClaimCalculator.claimByUnderwritingYear()` |
| 預期結果 | `{115: 126931, 114: 38331, 113: 32460}`；`Σ` = **197,722**；`M9.get(115)` **等於** `ClaimCalculator.totalClaim()` 之 126,931 |
| 驗收標準 | 全部相符 |
| 自動化 | 可 |

### TC-U-16 應產出年度清單（M10）

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證 R-CALC-18：**第一階段報表會產出時**排除等於設定年者、**降冪**排序 |
| 測試資料 | M9 + 設定年 115 |
| 執行步驟 | 呼叫 `ClaimCalculator.reportYears(M9, 115, true)` |
| 預期結果 | `[114, 113]`（**順序即為斷言之一**，非 `[113, 114]`） |
| 驗收標準 | 相符 |
| 自動化 | 可 |
| 備註 | **追加案例**：以人工構造之 M9 = `{116: 100, 115: 200}` 驗證 116（**大於**設定年）**仍須產出** —— 排除條件是「等於」而非「小於」 |

### TC-U-17 賠款 T 字帳金額配置

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證 R-CALC-19：四格同值、`O6`/`G14` 為 0、**不套用 M8** |
| 測試資料 | M9[114] = 38,331 |
| 執行步驟 | 取得該年度之寫入值集合 |
| 預期結果 | `G6` = `G21` = `O20` = `O21` = **38,331**；`O6` = `G14` = **0** |
| 驗收標準 | 相符 |
| 自動化 | 可 |
| **反向斷言** | `ManagementFeeCalculator.balanceDue(0, 38331, 0)` = **−38,331**，且**不等於** `G21` 之 38,331 —— 確保未誤套 M8（風險 K11） |

### TC-U-18 賠款 T 字帳之 U/Y 西元年

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證 R-CALC-20：西元年來源為**簽單年度**而非設定年 |
| 測試資料 | 設定年 115、簽單年度 113 / 114 |
| 執行步驟 | 計算各年度之 `P4` 值 |
| 預期結果 | 113 → `2024`、114 → `2025`；**皆不等於**第一階段之 `2026` |
| 驗收標準 | 相符 |
| 自動化 | 可 |

### TC-U-19 保費檔缺檔時 M10 不排除設定年（P-13）

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證 R-CALC-18 之**條件式排除**：前兩張報表不產出時，設定年之賠款無人承載，必須一併產出 |
| 測試資料 | M9 = `{115: 126931, 114: 38331, 113: 32460}`、設定年 115 |
| 執行步驟 | 呼叫 `ClaimCalculator.reportYears(M9, 115, false)` |
| 預期結果 | **`[115, 114, 113]`**（設定年一併列入且維持降冪）；`Σ M10` = **197,722** = `Σ M9` |
| 驗收標準 | 相符 |
| 自動化 | 可 |
| **追加案例** | **兩種模式恰為互補**：`premiumReportsProduced = true` → `M3 + Σ M10` = 126,931 + 70,791 = **197,722**；`= false` → `0 + Σ M10` = **197,722**。任一模式下賠款皆**不重複、不遺漏**（BR-23 賠款承載完整性） |

> 對照組（保費檔存在時設定年須排除）見 **TC-N-30**。

---

## 4. 端到端整合測試

### TC-E2E-01 標準月產製

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證完整流程與兩張報表全部輸出值 |
| 測試資料 | `檔案位子範例/` 完整目錄，設定年月 115/5 |
| 執行步驟 | 1. 準備 config / input / templet<br>2. 執行 `ReportGenerationService.execute()`<br>3. 以 POI 讀回兩張產出檔 |
| 預期結果 | **T 字帳**：I3=`115 年 05 月`（單格）、O4=`U/Y:`（未被覆寫）、P4=`2026`、G6=126,931、O6=350,123、G14=**21,009**、G20=**202,183**、G21=350,123、O21=350,123；工作表名 `共保月帳單-T字帳`<br>**彙整表**：A7:A22 為 16 家原樣板名稱、B15=350,123、C15=126,931、B23=350,123、C23=126,931、E23=100%、I23=**21,009**、F23=−350,123、**G23=+126,931**、D23=**223,192**、F22 公式 `=-1*$B$23-SUM(F7:F21)`、**G22 公式 `=ROUND($C$23-SUM(G7:G21),0)`**、F27 為空；工作表名 `共保月帳單-彙整表` |
| 驗收標準 | 全部相符；exit code = 0 |
| 自動化 | 可 |

### TC-E2E-02 無理賠月

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證理賠檔缺檔不視為錯誤 |
| 測試資料 | 同 TC-E2E-01，但移除 `VOLC11505.csv` |
| 執行步驟 | 執行後讀回兩張報表 |
| 預期結果 | 仍產出兩張報表；G6=0 且顯示 `-`；彙整表 C 欄與 G 欄全為 0；Balance Due = 350,123 − 0 − 21,009 = **329,114** |
| 驗收標準 | 相符；exit code = 0 |
| 自動化 | 可 |

### TC-E2E-03 跨報表一致性

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證 R-CALC-16 三組約束 |
| 測試資料 | TC-E2E-01 之產出 |
| 執行步驟 | 讀回兩張報表比對 |
| 預期結果 | T 字帳 G6 = 彙整表 C23（126,931）；O6 = B23（350,123）；G14 = I23（**21,009**） |
| 驗收標準 | 三組皆相等 |
| 自動化 | 可 |

### TC-E2E-04 儲存格格式

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證千分號與 `-` 顯示 |
| 測試資料 | TC-E2E-01 與 TC-E2E-02 之產出 |
| 執行步驟 | 讀回各金額格之 `number_format` |
| 預期結果 | 兩表所有金額格為 `_-* #,##0_-;\-* #,##0_-;_-* "-"??_-;_-@_-`；TC-E2E-02 中值為 0 之格顯示 `-`；樣板既有合併儲存格、框線、欄寬未被破壞 |
| 驗收標準 | 相符 |
| 自動化 | 可 |

### TC-E2E-05 公式寫入

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證計算欄位為公式而非數值 |
| 測試資料 | TC-E2E-01 之彙整表 |
| 執行步驟 | 讀取各格 cell type |
| 預期結果 | D/F/G/H/I/J 欄（第 7~22 列）與 B23:J23 為 **FORMULA**；B/C/E 欄為 **NUMERIC**；A 欄為 STRING 且值與樣板相同 |
| 驗收標準 | 相符 |
| 自動化 | 可 |

### TC-E2E-06 命令列參數覆寫

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證 R-RUN-04 |
| 測試資料 | 設定檔年月 115/5 |
| 執行步驟 | 分別以 `--year=115 --month=5` 與不帶參數執行 |
| 預期結果 | 兩次產出之報表內容完全相同；`report.json` 之 `paramOverride` 分別為 true / false |
| 驗收標準 | 相符 |
| 自動化 | 可 |

### TC-E2E-07 備份與逾期清除

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證 R-PATH-06 |
| 測試資料 | 先執行一次產出；另建立 4 個月前之假備份目錄 |
| 執行步驟 | 1. 第一次執行<br>2. 建立 `./backup/11505/{4個月前時間戳}/`<br>3. 第二次執行 |
| 預期結果 | 前次兩張報表出現於 `./backup/11505/{本次時間戳}/`；4 個月前之目錄已被刪除；`report.json` 之 `backupFiles` 與 `purgedBackups` 有對應紀錄 |
| 驗收標準 | 相符 |
| 自動化 | 可 |

### TC-E2E-08 執行報告內容

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證 report.json 結構與保留策略 |
| 測試資料 | TC-E2E-01 |
| 執行步驟 | 執行兩次後讀取 `./logs/report.json` |
| 預期結果 | 含 F1~F10 全部欄位；`summary` 之四項為 350,123 / 126,931 / **21,009** / **202,183**；檔案僅含**最後一次**執行內容 |
| 驗收標準 | 相符 |
| 自動化 | 可 |

### TC-E2E-09 賠款 T 字帳標準產出（第二階段）

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證多份輸出之檔數、檔名與逐格內容 |
| 測試資料 | `檔案位子範例/` 完整目錄（含 `CLAIM_T_ACCOUNT.xlsx`），設定年月 115/5 |
| 執行步驟 | 1. 執行 `ReportGenerationService.execute()`<br>2. 列出 `./output/11505/` 全部檔案<br>3. 以 POI 讀回兩份賠款 T 字帳 |
| 預期結果 | **共 4 檔**；賠款 T 字帳檔名為 `共保理賠_當月賠款月帳單_T字帳報表11505_114年.xlsx` 與 `..._113年.xlsx`<br>**113 份**：`I3`=`115 年 05 月`（單格）、`J3` 為空、`O4`=`U/Y:`（未覆寫）、`P4`=`2024`、`G6`=`G21`=`O20`=`O21`=**32,460**、`O6`=`G14`=**0**、**`G20` 為空**、工作表名 `共保賠款月帳單-T字帳`<br>**114 份**：同上但 `P4`=`2025`、四格皆 **38,331** |
| 驗收標準 | 全部相符；exit code = 0 |
| 自動化 | 可 |
| 備註 | **`G20` 為空**是關鍵斷言（風險 K12）；兩份之 `P4` 與金額須互不相同（風險 K13） |

### TC-E2E-10 賠款 T 字帳格式與工作表

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證會計格式與工作表更名 |
| 測試資料 | TC-E2E-09 之產出 |
| 執行步驟 | 以 POI 讀取 `G6` / `G21` / `O20` / `O21` / `O6` / `G14` 之 `CellStyle` |
| 預期結果 | 六格之 `DataFormatString` 皆為 `_-* #,##0_-;\-* #,##0_-;_-* "-"??_-;_-@_-`；`O6` / `G14` 值為 0，Excel 顯示 `-`；工作表名 `共保賠款月帳單-T字帳`（**不帶年度**，兩份同名） |
| 驗收標準 | 相符 |
| 自動化 | 可 |

### TC-E2E-11 賠款 T 字帳之備份與執行報告

| 項目 | 內容 |
| --- | --- |
| 測試目的 | 驗證 N 份輸出納入備份與 `report.json`（P-08） |
| 測試資料 | 連續執行兩次 |
| 執行步驟 | 1. 第一次執行產出 4 檔<br>2. 第二次執行<br>3. 檢視 `./backup/11505/{時間戳}/` 與 `report.json` |
| 預期結果 | 備份目錄含**全部 4 檔**（含兩份賠款 T 字帳）；`report.json` 之 `outputFiles[]` 有 **4 筆**；`summary` 含 `claimByUnderwritingYear` 與 `reportYears`（`[114, 113]`） |
| 驗收標準 | 相符 |
| 自動化 | 可 |
| 備註 | 備份清單若寫死兩檔，本案例會失敗（風險 K14） |

---

## 5. 反向／例外測試

| 編號 | 測試目的 | 測試資料／操作 | 預期結果 | 自動化 |
| --- | --- | --- | --- | --- |
| **TC-N-01** | 保費檔缺檔（**P-12 修訂**） | 移除 `VOLP11505.csv` | **不中止**；T 字帳與彙整表不產出、**賠款 T 字帳照常產出**；exit code = **0**。逐項斷言見 **TC-N-25**（修訂前為「中止、不產出任何報表、exit code = 2」，已隨 R-EXC-03 作廢） | 可 |
| **TC-N-02** | 設定檔缺檔 | 移除 `config/application.xlsx` | 中止；exit code = 2 | 可 |
| **TC-N-03** | 成分合計 ≠ 100% | 將「全球人壽」由 7.5% 改為 10%（合計成為 **102.5%**） | 中止並回報合計值（訊息含 `102.5%` 與 `100%`）；不產出報表 | 可 |
| **TC-N-04** | 公司代號重複 | 設定檔置入兩筆 `N05` | 中止並回報重複代號 | 可 |
| **TC-N-05** | 表頭欄位錯置 | 保費檔調換兩欄表頭順序 | 中止並回報表頭不符；exit code = 2 | 可 |
| **TC-N-06** | 表頭欄數不符 | 保費檔刪除一欄 | 中止 | 可 |
| **TC-N-07** | 編碼錯誤 | 將 CSV 轉存為 UTF-8 | 中止（表頭驗證失敗） | 可 |
| **TC-N-08** | 出生日期 6 碼 | 理賠檔改回 `671116` | 檢核失敗，錯誤含 `[R-VAL-02-11]` 與行號；不產出報表；exit code = 1 | 可 |
| **TC-N-09** | 年月不一致 | 設定月改為 6 | 全部資料列報錯（2,193 + 14 筆）；不產出報表 | 可 |
| **TC-N-10** | 公司代號不存在於設定檔 | 保費檔置入 `N99` | 檢核失敗並回報該列 | 可 |
| **TC-N-11** | 多筆錯誤全部列出 | 保費檔注入 5 筆不同欄位之錯誤 | `report.json` 之 `errorCount` = 5 且逐筆含**六要素**；未於第一筆中止 | 可 |
| **TC-N-12** | 個資遮蔽 | 觸發 TC-N-08 | 主控台、日誌、report.json 中身分證號皆為 `A12****789` 格式；無明碼 | 可 |
| **TC-N-13** | 樣板名稱查無對應 | 設定檔將「華南產險」改名為「華南產物」 | 中止並回報樣板第 20 列名稱「華南產險」查無對應（R-EXC-06）；exit code = 2 | 可 |
| **TC-N-14** | 樣板檔缺檔 | 移除 `PREMIUM_SUMMARY.xlsx` | 中止；exit code = 2 | 可 |
| **TC-N-15** | 備份失敗 | 產出前將既有輸出檔設為唯讀／鎖定 | 中止且**原檔未被覆寫**；exit code = 2 | 可 |
| **TC-N-16** | 參數格式錯誤 | `--month=13` | 中止並回報參數錯誤 | 可 |
| **TC-N-17** | 保費合計不等於三項相加 | 保費檔改一列之保費合計 | 檢核失敗並回報 `[R-VAL-01-18]` | 可 |
| **TC-N-18** | 被保險人數 = 0 | 保費檔改一列為 0 | 檢核失敗並回報 `[R-VAL-01-07]` | 可 |
| **TC-N-19** | 身故殘廢保額超限 | 改一列為 1,000,001 | 檢核失敗並回報 `[R-VAL-01-12]` | 可 |
| **TC-N-20** | 批單號碼條件必填 | 批單起日有值但批單號碼空白 | 檢核失敗並回報 `[R-VAL-01-05]` | 可 |
| **TC-N-21** | **全部簽單年度皆為設定年** | 理賠檔改為 14 列全部 `簽單年度 = 115` | `M10` 為空；**賠款 T 字帳產出 0 份**；T 字帳與彙整表**照常產出**（共 2 檔）；**exit code = 0**；`report.json` 附「無非當年度簽單資料」訊息 | 可 |
| **TC-N-22** | **理賠檔缺檔時之賠款 T 字帳** | 移除 `VOLC11505.csv` | 產出 **2 檔**；賠款 T 字帳 0 份；**exit code = 0**；訊息可與 TC-N-21 **區分**（缺檔 vs 無非當年度資料） | 可 |
| **TC-N-23** | **賠款樣板缺檔（需產出時）** | 移除 `CLAIM_T_ACCOUNT.xlsx`，理賠檔含 113/114 | 中止；exit code = 2；訊息指出樣板路徑 | 可 |
| **TC-N-24** | **賠款樣板缺檔（不需產出時）** | 移除 `CLAIM_T_ACCOUNT.xlsx`，理賠檔全為 115 年 | **不中止**；照常產出 2 檔；exit code = 0（驗證 D14 之延後檢查） | 可 |
| **TC-N-25** | **保費檔缺檔，理賠檔含 115／114／113**（P-12 + P-13） | 移除 `VOLP11505.csv` | 產出 **3 檔**（賠款 T 字帳 **115**／114／113）；T 字帳與彙整表**不存在**；`M10` = `[115, 114, 113]`；115 份之 `P4` = `2026`、四格金額 = **126,931**；三份合計 **197,722**；`M1` = `M7` = 0；**exit code = 0**；`report.json` 之 `premiumFileMissing` = `true`、訊息含「保費匯入檔不存在」 | 可 |
| **TC-N-26** | **保費檔缺檔不得備份掉前次產出**（P-12） | 先正常執行一次（4 檔），再移除 `VOLP11505.csv` 重跑 | 前次之 T 字帳與彙整表**仍留在** `./output/11505/`（未被 `Files.move()` 移走）；`backupFiles` 僅 **2 筆**（前次已存在之 114／113；115 份為新檔無舊檔可備份）；`outputFiles` **3 筆**；exit code = 0 | 可 |
| **TC-N-27** | **保費檔與理賠檔皆缺檔** | 兩檔皆移除 | **中止**；exit code = 2；訊息含 `R-EXC-03` 與「理賠匯入檔亦不存在」；`./output/11505/` **不存在** | 可 |
| **TC-N-28** | **保費檔缺檔且全部簽單年度皆為設定年**（P-13） | 移除 `VOLP11505.csv`，理賠檔改為全部 `簽單年度 = 115` | **不中止**；產出 **1 檔**（115 年）；`M10` = `[115]`；該份金額 = **197,722**；`P4` = `2026`；exit code = 0。**設定年不被排除，因前兩張未產出** | 可 |
| **TC-N-29** | **保費檔僅有表頭（空檔 ≠ 缺檔）** | 將 `VOLP11505.csv` 截成只剩表頭列 | **不中止**；產出 **4 檔**；`premiumFileMissing` = `false`；**設定年仍被排除**（前兩張有產出）；`M1` = `M7` = 0；`M3` 仍為 **126,931**；Balance Due = **−126,931**（負值照實寫入） | 可 |
| **TC-N-30** | **保費檔存在時設定年須排除**（P-13 對照組） | 標準樣本，不做任何破壞 | `M10` = `[114, 113]`；`./output/11505/` **無** `..._115年.xlsx`；`M3 + Σ M10` = **197,722** = 理賠檔全部已決賠款 | 可 |

---

## 6. 手動驗收

| 編號 | 測試目的 | 執行步驟 | 預期結果 |
| --- | --- | --- | --- |
| **TC-M-01** | fat jar 部署 | 於未安裝 Maven 之 JDK 17 機器雙擊 jar | GUI 視窗正常開啟 |
| **TC-M-02** | GUI 正常流程 | 於畫面指定年月 → 執行 | 顯示執行成功摘要與輸出檔路徑 |
| **TC-M-03** | GUI 錯誤呈現 | 以 TC-N-08 之資料執行 | 畫面表格列出全部錯誤（含五要素、個資已遮蔽）；無匯出按鈕 |
| **TC-M-04** | CLI 批次 | 執行 `拜託執行我.bat`（含編譯） | 主控台輸出摘要；報表正確產出 |
| **TC-M-05** | 報表目視檢查 | 開啟**全部四張**產出報表 | 版面與樣板一致；金額有千分號；0 值顯示 `-`；公式可於 Excel 重算且值正確；賠款 T 字帳之 `O6` / `G14` 顯示 `-`、`G20` 空白 |
| **TC-M-06** | 效能 | 以標準資料執行並計時 | 30 秒內完成（NF-05，門檻為【推論】） |
| **TC-M-07** | 賠款 T 字帳與範例目視比對 | 開啟產出之 113 / 114 兩份，與 `第二階段-賠款月帳單/產出範例/` 並排 | 金額（32,460 / 38,331）與 `G21`/`O20`/`O21` 配置**相同**；`I3`/`J3`、`O4`/`P4`、`O6`/`G14` 之寫法**不同**（依 P-01/P-02/P-05 定案，範例為舊版） |

---

## 7. 驗收標準彙總

> **兩個數字不要混淆**：下表之 19 / 11 / 30 / 7 是本文件定義的**測試案例**數（合計 67 項，含 7 項手動驗收）；版本紀錄中的「測試總數 **56** 項」是 `mvnw.cmd -o test` 實跑的 **JUnit 測試方法**數。兩者不是一對一——多個案例可由同一個測試方法以追加斷言涵蓋，手動驗收（`TC-M-*`）則不計入 JUnit。

| # | 驗收項目 | 對應案例 |
| --- | --- | --- |
| AC-01 | 全部 `TC-U-*`（**19 項**）通過 | §3 |
| AC-02 | 全部 `TC-E2E-*`（**11 項**）通過 | §4 |
| AC-03 | 全部 `TC-N-*`（**30 項**）通過 | §5 |
| AC-04 | 全部 `TC-M-*`（**7 項**）通過 | §6 |
| AC-05 | 核心計算類別行覆蓋率 ≥ 80%【推論】 | T-19 |
| AC-06 | 測試程式中**未出現**第一階段產出範例檔之 5% 版舊值（17,505 / 205,687 / 1,226）與 12% 版舊基準 | 靜態掃描（`StaticGuardTest`） |
| AC-07 | REQ §10 之 AC-01 ~ AC-32 皆可對應至本文件之測試案例 | 逐項比對 |
| **AC-08** | 第二階段之 4 檔產出、`G20` 為空、`P4` 來源為簽單年度，皆有專屬斷言 | TC-E2E-09 / TC-U-18 |
| **AC-09** | 「產 0 份仍成功」之兩種原因（TC-N-21 / TC-N-22）訊息可區分且 exit code 皆為 0 | 反向測試 |
| **AC-10** | 測試程式中**未出現** −38,331（誤套 M8 之結果）作為期望值 | 靜態掃描 |
| **AC-11** | 保費檔缺檔之四種組合（TC-N-25 ~ TC-N-28）行為與 exit code 皆符合 R-EXC-03 修訂版；「缺檔」與「空檔」（TC-N-29）結果不同且可由 `premiumFileMissing` 區分 | 反向測試 |
| **AC-12** | **賠款承載完整性**（BR-23）：TC-U-19、TC-N-25、TC-N-28、TC-N-30 各自驗證「第一階段承載 + 賠款 T 字帳承載 = 197,722」，任一模式皆不重複、不遺漏 | 單元 + 反向測試 |
| **AC-13** | 測試程式中**未出現**「保費檔缺檔時 M10 = `[114, 113]`」之期望值——那是 P-13 修訂前的錯誤行為，會讓 115 年之 126,931 憑空消失 | 逐項比對 |

---

## 8. 版本紀錄

| 版本 | 日期 | 內容 |
| --- | --- | --- |
| **v1.5** | **2026-08-14** | **文件一致性校正（無測試變更）**：① **TC-N-01「保費檔缺檔」依 P-12 改寫**——原列「中止；不產出任何報表；exit code = 2」為 R-EXC-03 修訂前之舊行為，與同一張表之 **TC-N-25**（同樣移除 `VOLP11505.csv`，卻是產出 3 檔、exit code 0）**直接矛盾**；改為「不中止、前兩張不產、賠款 T 字帳照產、exit code 0，逐項斷言見 TC-N-25」；② 文件資訊之目前版本／最後更新補正（原停留 v1.2 / 2026-08-10）；③ **TC-U-19 移至 TC-U-18 之後**（原插在 TC-U-16 與 TC-U-17 之間）並補上 TC-N-30 對照組指引、**TC-M-07 移至 TC-M-06 之後**；④ §7 加註「測試案例 67 項」與「JUnit 測試方法 56 項」之區別，避免兩個數字被誤讀為打架 |
| **v1.4** | **2026-08-13** | **P-13：M10 之排除設定年改為條件式**。新增單元測試 **TC-U-19**（含「兩種模式互補」追加案例）與反向測試 **TC-N-30**（保費檔存在時設定年須排除之對照組）；**TC-N-25 由 2 檔改為 3 檔**（含 115 年、金額 126,931、`P4` = 2026）、**TC-N-28 由「中止」改為「產出 1 檔且成功」**、TC-N-26 補上 `outputFiles` 3 筆之斷言、TC-N-29 補上「設定年仍被排除」；TC-U-16 之呼叫改為三參數；驗收彙總改為 **19 / 11 / 30 / 7** 項並新增 **AC-12 / AC-13**。實測結果：測試總數 53 → **56 項**，全數通過 |
| **v1.3** | **2026-08-13** | **P-12：新增反向測試 TC-N-25 ~ TC-N-29**（保費檔缺檔只產賠款 T 字帳、缺檔時不得備份掉前次產出、雙檔皆缺中止、缺檔且無可產出年度中止、保費檔僅有表頭仍照產）；驗收彙總 `TC-N-*` 由 24 項改為 **29 項**、AC-07 對應範圍改為 REQ AC-01 ~ AC-32、新增 **AC-11**。實測結果：測試總數 48 → **53 項**，全數通過 |
| **v1.2** | **2026-08-10** | **新增第二階段（賠款 T 字帳）測試規格**：§2.2 新增第二階段基準值表（M9 / M10 / 四格金額 / 4 檔 / 跨階段不變式）與兩項禁止事項；新增單元測試 **TC-U-15 ~ TC-U-18**（分群、年度清單含「大於設定年仍須產出」追加案例、金額配置含 M8 反向斷言、U/Y 來源）；新增整合測試 **TC-E2E-09 ~ TC-E2E-11**（4 檔逐格斷言含「`G20` 為空」、格式與工作表、備份與 report.json）；新增反向測試 **TC-N-21 ~ TC-N-24**（全為設定年、理賠檔缺檔、樣板缺檔之需要／不需要產出兩種情境）；新增手動驗收 **TC-M-07**；驗收彙總更新為 18 / 11 / 24 / 7 項並新增 AC-08 ~ AC-10 |
| v1.1 | 2026-08-10 | 依 2026-08-10 業務調整同步全部驗收數值：管理費率 6%、成分 7.5%/7%、G 欄改正值、D 欄改 `B−C`。基準值更新為 M5[N19] **24,512**、M7 **21,009**、M8 **202,183**、G22 **+8,882**、I22 **1,471**、I15 **1,260**；TC-N-03 明訂以 102.5% 觸發；TC-E-04 缺理賠檔之 Balance Due 改為 **329,114**；TC-M-04 改執行 `拜託執行我.bat` |
| v1.0 | 2026-08-03 | 初版；14 項單元測試、8 項端到端測試、20 項反向測試、6 項手動驗收，共 48 案；明確標示驗收數值基準並禁止以產出範例檔金額比對 |
