# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 專案狀態

規格先行的專案，分兩階段：

- **第一階段（`dev`，已上線）**：REQ / DESIGN / TASK / TEST 皆已完成，**實作已完成並通過驗收**（T-01 ~ T-21；T-22 取代產出範例檔待業務方確認）。交付物為一支讀取共保系統匯出的 CSV、套用 Excel 樣板、產出兩張月帳單報表（T 字帳、彙整表）的程式，位於 `src/main/java/com/insurance/coinsurance/`。
- **第二階段（`feature/phase-two`）**：新增第三張報表「**當月賠款月帳單－T字帳**」——只讀理賠檔，**排除簽單年度 == 設定年**，其餘每個簽單年度各產出一份。**M0 ~ M6 全部完成**（2026-08-12）：P-01 ~ P-11 結案，文件全面納入第二階段，**T-23 ~ T-28 已實作並通過驗收**（`ClaimTAccountCell` / `ClaimYearSummary` / `ClaimCalculator` 擴充 / `ClaimTAccountWriter` / 服務層編排 / 進入點訊息）。實跑產出 4 檔、exit code 0、金額 32,460（113）與 38,331（114）。2026-08-13 追加 **T-30（P-12：保費檔缺檔時之部分產出）** 與 **T-31（P-13：`M10` 條件式排除設定年，PM 明示）**，測試 48 → **56 項**。改動前先讀 `..._LOG_第二階段問題追蹤清單.md` §5、MAPPING §10、RULE 之 R-PATH-07 / R-CALC-16~20 / R-OUT-08~09 / **R-EXC-03**、DESIGN §4.2 與 D10 ~ **D19**、TASK 之 K11 ~ **K18**。
- **第二階段·第四張報表「當月共保賠款月帳單－彙總表」（2026-08-17 新增規格）**：性質是「第一階段彙整表的逐簽單年度、保費恆為 0 版本」：只讀理賠檔，`B`/`F`/`I` 三欄恆 0、`C` 欄為該簽單年度依公司分群之已決賠款、`G` 欄依成分分攤（中央再保仍為差額法）、合計列 `J23` 恆為 0。**M7 ~ M12 全部完成**（2026-08-18）：P-14 ~ P-23 全數結案（PM 回覆見 `已確認回覆問題6.txt`，9 題採建議、P-21 未答依建議定案），六份常駐文件全面納入（定案規則 `..._LOG_第二階段問題追蹤清單.md` §5.4、MAPPING §11 含 `M12`、RULE 之 **R-PATH-08 / R-CALC-21~24 / R-OUT-10**、REQ §6.5 與 **BR-24~32**、DESIGN §4.3 與 **D20~D23**、TASK **階段八**、TEST **TC-U-20 / TC-E2E-12~14 / TC-N-31~35**），**T-32 ~ T-37 已實作並通過驗收**（`ClaimSummaryCell` / `ClaimCalculator.claimByYearAndCompany` / **`SummarySheetPainter`** / `ClaimSummaryWriter` / 服務層編排 / 測試）。測試 56 → **63 項**，實跑產出 **6 檔**、exit code 0、`J23` = 0。
  - **與第三張報表共用 `M10`**（含 D9 之條件式排除設定年），故份數永遠一致；**年度一律取簽單年度**（`A3` 之 U/Y、`C` 欄、檔名），**只有 `E3` 取設定檔年月**。輸出檔名 `共保理賠_當月賠款月帳單_彙總表{YYYMM}_{簽單年度}年.xlsx`，工作表名 `共保賠款月帳單-彙總表`。一次執行共 **2 + N×2 = 6 檔**。
  - **公式一字都沒改**：`F` / `I` 與中央再保差額法三組公式皆以 `$B$23` 為輸入，`B` 欄餵 0 就自動全為 0。公式邏輯已抽成 **`writer/SummarySheetPainter`**，第一階段彙整表與第四張報表共用；**改任一張的公式就是同時改兩張**，動它之前先確認兩邊都該改。
  - **⚠️ 最容易踩且最難察覺的雷（K20）**：`G` 欄（應攤配賠款）的分攤基準是**該年度合計 `C23`**，不是各公司自己的 `M12[y][code]`。誤用的話 —— **`C23` / `G23` / `J23` 三個合計仍然全對**，因為中央再保的差額法會把差異全數吸收，只有 `G` 欄逐家金額會露餡（15 家歸零）。**測試必須逐家斷言 16 列**，`J23 == 0` 這條零和檢查擋不住它。
- **業務方確認狀態**：**P-01 ~ P-23 全數結案**（P-12 / P-13 於 2026-08-13、P-14 ~ P-23 於 2026-08-18；B27 / Q-23 隨 P-12/P-13 修訂）。**T-22 / T-29 / T-38 產出範例取代已於 2026-08-18 執行完畢**（依 P-23 之同意）。**目前無待業務方確認之阻斷項目。**

第二階段一律在 `feature/phase-two` 開發，驗收後才合回 `dev`。

- **GUI 已移出（2026-08-22，TASK T-40）**：`entry/FxLauncher`、`entry/FxApplication`、`entry/MainController`、`開啟畫面.bat` 與 `pom.xml` 之三個 JavaFX 依賴都在 **`feature/gui`**，本分支沒有。fat jar 主類別是 **`entry/CliLauncher`**，`java -jar` 直接進批次；`--mode=cli` **刻意保留相容**（業務方的 `.bat` 與排程都還帶著它），由 `CliRunner.parse` 忽略。報表邏輯兩分支同源——**`feature/gui` 只接收本分支的合併，不要在那邊改 `calculator/` 或 `writer/`**。文件不分家：十份常駐文件在文件資訊後各加一段「分支歸屬」提示，GUI 專屬條目（R-RUN-05、NF-06/07、T-18、TC-M-02/03、D8、K8、D-04/D-05）**保留不刪但標為本分支不適用**——刪掉的話日後回查業務回覆（B15 / B16 / N-09 / A-09）會對不上。

## 常用指令

```
build.bat                          建置 fat jar（內含指定 JDK 17 之 JAVA_HOME）
拜託執行我.bat [--year=115 --month=5]  編譯 + CLI 批次執行（原 run.bat，已併入 package）
mvnw.cmd -o test                   執行 63 項測試（-o 離線，相依已在本機倉庫）
mvnw.cmd clean package -DskipTests
```

`JAVA_HOME` 於本機為 `C:\Program Files\Amazon Corretto\jdk17.0.18_9`（PATH 上的預設 java 是 JDK 25）。
`拜託執行我.bat` 內另有一組寫死的 `JAVA_EXE`（`C:\Users\user\.jdks\corretto-17.0.18\bin\java.exe`），是**業務方機器**的路徑，在本機不存在——要跑批次請直接用 `mvnw.cmd` 或帶 `JAVA_HOME` 執行 jar。
執行環境目錄 `config/` `templet/` `input/` `output/` `backup/` `logs/` 位於專案根目錄；`input/`、`output/`、`backup/`、`logs/` 未納入版控。

## 程式架構

分層：`entry`（CLI；GUI 在 `feature/gui`）→ `service`（`ReportGenerationService`，唯一對外入口）→ `config` / `reader` / `validator` / `calculator` / `writer`，共用 `model` / `constant` / `util` / `exception`。

改動計算或輸出邏輯前，務必先讀下方「領域地雷」——每一條都有對應的測試在把關（`StaticGuardTest` 甚至會掃描原始碼）。

- **`CliLauncher` 是 fat jar 的主類別**（原為 `FxLauncher`，GUI 移出後改此）。`feature/gui` 那邊的 `FxLauncher` **刻意不繼承 `javafx.application.Application`**，直接繼承會讓 JavaFX 以「runtime components are missing」啟動失敗——改那個分支時別忘了。
- **`CliRunner` 刻意不是 `CommandLineRunner`**：原因是 CLI 與 GUI 曾共用同一個 Spring 容器，自動執行會讓 GUI 一開就跑批次。**GUI 移出後仍不要改回去**——批次的觸發時機屬於進入點，容器建立本身不該有副作用，測試也才能單獨組裝容器而不觸發整批產出。
- **`ReportGenerationService` 不得 `System.exit()` 或直接印訊息**，只回傳 `ExecutionResult`；中止行為由進入點決定。
- **捨入只能經 `calculator/RoundingUtil`**（固定 HALF_UP）。
- **`writer/SummarySheetPainter` 由第一階段彙整表與第四張報表共用**（公式字串單點存在）。改它等於同時改兩張報表；`SummaryWriter` / `ClaimSummaryWriter` 只負責表頭、資料來源與命名常數。

## 文件體系

`docs/` 下**十份常駐文件**（下表列出改動最頻繁的五份；另有 `REQ_需求規格` / `DESIGN_系統設計` / `TASK_任務拆分` / `TEST_測試驗收`，以及 **`GUIDE_文件與資料索引`——全部文件與資料的地圖，找不到東西時先看它**）。每個主題只有一份檔案，改版時**直接更新該檔**並在文件內的「版本紀錄」加一列。**不要建立同名不同版的檔案**（如 `_v2.0_20260803.md`）——這是使用者明確要求過的。

**兩階段共存的寫法**：常駐文件不分家，各自加「第二階段」章節並升版（現況分析 §10 / §11、MAPPING §10 / §11、RULE 之 R-*-第二階段條目、REQ §6.4 / §6.5 與 UC-06 / UC-07、DESIGN §4.2 / §4.3）。只有問題追蹤清單因 Q/N/R/C 與 P 兩套編號互不相干，才拆成兩份。

| 檔案 | 內容 |
| --- | --- |
| `..._LOG_現況分析與輸入輸出盤點.md` | 實測事實基線、樣板/設定檔逐格內容、驗算結果、34 項已定案業務決策（B01–B34） |
| `..._MAPPING_欄位對照.md` | CSV 欄位索引、中間模型（M1~M8 一階段、M9~M11 第三張、**M12 第四張**）、**四張報表**的儲存格對照（§7 / §8 / §10 / **§11**）、report.json 規格（F1~**F13**） |
| `..._RULE_規則定義.md` | 可實作規則（`R-PATH-` / `R-VAL-` / `R-CALC-` / `R-OUT-` / `R-EXC-` / `R-RUN-`）與執行順序 |
| `..._LOG_問題追蹤清單.md` | 第一階段：Q-01~Q-26、N-01~N-11 結案狀態、R-01~R-09 殘留事項，以及 C-01~C-05 實作後業務調整 |
| `..._LOG_第二階段問題追蹤清單.md` | **第二階段**：第三張報表 P-01~P-13（**全數結案**）、**§5 定案規則**（含儲存格對照）、M0~M6 里程碑、113/114 驗收基準；**第四張報表 §3.2 事實盤點 + P-14~P-23（✔ 全數結案）+ §5.4 定案規則（D11~D15）+ M7~M12（全部完成）** |

**規格來源**（2026-08-10 重整，原 `docs/規格書/` 已改名；**2026-08-18 兩階段統一分層**）：

```
docs/規格來源/
  第一階段-共保月帳單/
    規格書/          T字帳 v1.2、彙總表 v1.2（各 .md/.docx）、版面說明檔
    匯入檔範例/      VOLP11505.csv、VOLC11505.csv（與下方 input/ 內容相同）
    產出範例/        ★2026-08-18 已以實跑輸出取代
    檔案位子範例/    config・templet（四份樣板齊全）・input・產出範例  ★測試基準，勿搬動
  第二階段-賠款月帳單/
    規格書/          T字帳 v1.0、彙總表 v1.0（各 .md/.docx）
    範本/            CLAIM_T_ACCOUNT.xlsx、CLAIM_SUMMARY.xlsx（★2026-08-18 修訂版）
    產出範例/        T字帳 113/114、彙總表 113/114（★實跑輸出）、
                     共保保費_當月共保月帳單_彙整表11505_115年.xlsx（❗PM 隨附，非本程式產出）
    範例圖檔/        T字帳版面標記說明 ×2、共保理賠_當月賠款月帳單.jpg
    業務回覆/        待確認問題回覆5.txt、待確認問題6.txt、已確認回覆問題6.txt
```

**逐檔用途、可否作驗收基準、容易誤用之處，見 `..._GUIDE_文件與資料索引.md`。**

`TestFixtures.SAMPLE_ROOT` 寫死 `docs/規格來源/第一階段-共保月帳單/檔案位子範例`——搬動該目錄要同步改這行。

`簡易回覆爭議v0.2~v0.4.txt`（業務方逐題回覆）已於 commit `6448b84` 移除，結論全數保存於上表兩份 LOG；需原文用 `git show 6448b84^:docs/簡易回覆爭議v0.4.txt`。

**衝突時的優先順序**：業務回覆（新 > 舊）> 實體檔案實測 > 規格書文字 > 產出範例檔。**規格書仍殘留舊值**（第二階段 `.docx` 未同步 `.md`），見下方「地雷」。

`.docx` 規格書與 `.md` 目前不同步（`.md` 已更新成分表，`.docx` 未更新）——見問題追蹤清單 R-05。

## 領域地雷（實際踩過，改動計算邏輯前務必確認）

- **CSV 是 Big5 / CP950，含表頭列**。保費檔 19 欄、理賠檔 22 欄。兩檔都有**兩個欄位叫「日額」**，理賠檔的「已決賠款 合計」中間有半形空白 → **必須依位置索引讀取，不可依欄名**。
- **理賠檔第 6 欄「簽單年度」是核心篩選欄位**，v1.0 規格書漏列過。「攤付共保賠款」＝簽單年度 == 設定年 的已決賠款合計，**只篩年、不篩月**。不篩選會得到 197,722（錯），正確是 126,931。
- **中央再保（代號 `N19`）採差額法**：分攤金額 = 總額 − 其餘公司加總，**不是** 總額 × 成分。判定要**依公司代號**，不可依「最後一列」。
- **設定檔共保成分合計必須等於 100%，程式要檢核，不符即中止**。曾經出現過 105%（「全球人壽 L64」誤植為 10%），那是資料錯誤、不是刻意設計。中央再保的差額法只用來吸收其餘公司**四捨五入的尾差**（本月 3 元），不是用來吸收百分比缺口。
- **現行成分是壽險 8 家各 7.5%、中央再保 7%、產險 7 家 33%**（2026-08-10 由 55%/12%/33% 調整）。`docs/規格來源/第一階段-共保月帳單/檔案位子範例/config/application.xlsx`（測試基準）已同步為此版。
- **共保管理費率是 6%**（2026-08-10 由 5% 調整）。**四捨五入順序不可調換**：各公司分攤保費先四捨五入 → 再 ×6% → 再四捨五入 → 最後加總。先加總再乘會得到不同結果。全案一律 **HALF_UP**，Java 不可用 `BigDecimal` 預設的 HALF_EVEN。
- **彙整表列順序以樣板 `PREMIUM_SUMMARY.xlsx` 的 A7:A22 為準**（A 欄不覆寫），程式用**公司名稱**去設定檔查代號與成分。樣板 16 家與設定檔 16 家名稱目前 100% 對應。
- **保費合計含負值列**（批單沖銷，樣本 100 列），加總時不可過濾。也**不去重、不檢核重複**保單號／賠案號。
- **全部 `產出範例/` 已於 2026-08-18 以實跑輸出取代**（R-06 / T-22 / T-29 / T-38，PM 於 P-23 同意）。金額現已正確，**但驗收仍不得以範例反推**——範例是程式的產物，拿它當基準等於用程式驗證程式自己。基準一律取自文件（TEST §2.2）。`StaticGuardTest` 會掃描測試碼是否出現 5% 版舊值（17505 / 205687 / 1226）。
- 樣板 `PREMIUM_SUMMARY.xlsx` 的 **E7:E22 是空的**（只有格式），成分由程式在執行時依設定檔填入 —— 別把樣板和產出範例搞混。樣板 A6:J6 是**計算說明列**（`(4) = (2) - (3)`、`(9)=(6)x6%`），程式不覆寫，但改公式時要記得一起改樣板文字。
- **彙整表 F 欄（應分配保費）是負值、G 欄（應攤配賠款）是正值**，D 欄是 `=B{n}-C{n}`。G 欄的正負與 D 欄的加減都在 2026-08-10 翻過一次，`ReportGenerationServiceTest` 有斷言把關。
- **（第二階段）`CLAIM_T_ACCOUNT.xlsx` 跟 `PREMIUM_T_ACCOUNT.xlsx` 只差 A2 標題**（`共保賠款月帳單` vs `共保月帳單`），很容易誤以為可以共用 `TAccountCell` —— **不行**。業務方已定案（P-03 / P-04）：第二階段 **`G20` 留空、Balance Due 寫 `G21`、`O20` 也要寫**，且 `P4` 的西元年來源是**簽單年度**而非設定年。五處語意不同，**必須另立 `ClaimTAccountCell`**。
- **（第二階段）Balance Due 直接等於該簽單年度的賠款總和（正值），不是 `共保保費 − 賠款 − 管理費`**。誤套第一階段的 M8 會得 −38,331（符號相反）。
- **（第二階段）`M10` 的「排除設定年」是有條件的，不是絕對的**（P-13 / R-CALC-18，PM 2026-08-13 明示）。真正的規則是「**設定年之賠款已由第一階段報表承載，才不必重複出一份**」：保費檔存在 → 排除設定年（115 併入第一階段的攤付共保賠款）；**保費檔缺檔 → 不排除**，因為前兩張沒產出，該年賠款無人承載。樣本：保費檔缺檔時 `M10` = `[115, 114, 113]` 共 3 份、合計 197,722。**寫成無條件排除的症狀是 115 年的 126,931 憑空消失，而且帳面上看不出來** —— `verifyConsistency()` 的「賠款承載完整性」`Σ M9 ==（前兩張產出 ? M3 : 0）+ Σ M10`（D19）就是為此在執行期把關。
- **（第二階段）保費檔「缺檔」與「空檔」是兩回事**（P-12 / R-EXC-03）。缺檔 → 前兩張報表**不產**、賠款 T 字帳**照產**（它只讀理賠檔）、`report.json` 之 `premiumFileMissing` 為 `true`；三張全部落空（＝理賠檔也無任何資料列）才中止。保費檔存在但只有表頭（0 資料列）→ **不算缺檔**，三張照產、**設定年仍被排除**、Balance Due 為負值照實寫（PM 已確認共保系統實務上是「不匯出保費檔案」，空檔僅為防衛性設計）。**改動時最容易踩的兩個雷**：① 備份清單與寫出清單必須由同一個 `premiumFileExists` 旗標控制——`BackupService` 用 `Files.move()` 搬檔，備份了卻不重寫會把前次成果移走且不補回（D16）；② 「全部落空」的中止點必須在一致性檢查之後、**備份之前**，否則會留下有備份無產出的空目錄（D17）。
- **（第二階段·第四張報表）`CLAIM_SUMMARY.xlsx` 的第 6 列（計算說明列）原本是舊值，已於 2026-08-18 修正**：`D6` 曾是 `(4) = (2) + (3)`（應為 `-`）、`I6` 曾是 `(9)=(6)x5%`（費率已是 6%）。程式**不覆寫**第 6 列，錯字會原樣印在報表上。**PM 回覆 P-14「已調整」時只修了 `D6`，`I6` 是後來才由本專案補修的**——若日後又收到 PM 重發的範本，**務必先驗 `I6`**。現行三處（`templet/`、規格來源、`檔案位子範例/templet/`）已同版，與 `PREMIUM_SUMMARY.xlsx` 的差異只剩 `A2`。改範本請用 zip／XML 層級替換 `sharedStrings`，**別用 openpyxl 重存**（會掉列印設定與格式）。隨附的「範例檔」也不是本報表產的——A2 標題、工作表名、檔名全是第一階段彙整表的樣貌，**只有金額與儲存格配置可用**（P-20 已定案）。
- **（第二階段）產出範例的舊版面問題已消除**（原為 I3+J3 兩格、O4 含年、O6/G14 是字串 `-`）：2026-08-18 已以實跑輸出取代，現為 I3 單格、P4 年、數值 0、G20 留空（T-29）。**但 PM 隨規格書附的 `產出範例/共保保費_當月共保月帳單_彙整表11505_115年.xlsx` 仍是第一階段樣板所產**，那份不是本程式的輸出，只有金額與儲存格配置可用（P-20）。

### 驗收基準數字（115 年 5 月樣本）

**第一階段**

以 `docs/規格來源/第一階段-共保月帳單/匯入檔範例/VOLP11505.csv` + `VOLC11505.csv` + `檔案位子範例/config/application.xlsx`（16 家、合計 100%）計算：

| 項目 | 值 | 產出範例檔（**5% 版，勿用**） |
| --- | --- | --- |
| 共保保費 | 350,123 | 350,123 |
| 攤付共保賠款（簽單年度 115） | 126,931 | 126,931 |
| 共保管理費（費率 6%） | **21,009** | 17,505 |
| Balance Due | **202,183** | 205,687 |
| 中央再保應分配保費 | **−24,512** | −24,512（相同） |
| 中央再保應攤配賠款 | **+8,882** | −8,882（符號相反） |
| 中央再保應繳管理會費 I22 | **1,471** | 1,226 |
| 富邦產險應繳管理會費 I15 | **1,260** | 1,050 |

T 字帳寫入位置（規格書 v1.0 曾整體偏一列，已修正）：`I3` 單格 `115 年 05 月`、`P4` 只寫 `2026`（`O4` 的 `U/Y:` 是樣板既有、不覆寫）、`G6` / `O6` / `G14` / `G20` / `G21` / `O21`。

**第二階段·第三張報表 T 字帳**（已實作，實跑輸出與範例檔金額相符）：排除簽單年度 115 後產出 2 份 —— **114 年 → 38,331**、**113 年 → 32,460**。
交叉驗證：38,331 + 32,460 + 126,931（第一階段的 115 年）= 197,722 = 理賠檔全部金額。

**第二階段·第四張報表 彙總表**（已實作）：與 T 字帳同年度、同份數，一次執行共 **6 檔**。

| 儲存格 | 113 年 | 114 年 |
| --- | --- | --- |
| `A3` | `Ｕ/Y：2024` | `Ｕ/Y：2025` |
| `C15` = `C23` | 32,460 | 38,331 |
| `D15` | −32,460 | −38,331 |
| `G22` 中央再保（**差額法**） | **2,267**（7% 直算 2,272） | **2,680**（直算 2,683） |
| `J15` | −30,512 | −36,031 |
| **`J23`** | **0** | **0** |

`G` 欄逐家：113 → 壽險 8 家各 2,435、富邦/明台/國泰產險各 1,948、和泰 974、泰安 649、新光/華南產險各 1,623；114 → 各 2,875 / 2,300 / 1,150 / 767 / 1,917。`B` / `F` / `I` 三欄含合計恆 0。

## 本機工具鏈

- **`python` / `python3` 在 PATH 上是 Windows Store 的壞存根（exit 49）**。要讀 xlsx 或 Big5 CSV 請用絕對路徑：`/c/Users/astra/anaconda3/python.exe`（已含 openpyxl 3.1.5、pandas 2.2.3）。
- 讀 xlsx 需分別以 `data_only=False`（取公式）和 `True`（取快取值）各載入一次，否則看不到公式。
- **Git Bash 沒有 `iconv`**。Big5 解碼用 Python 的 `encoding='cp950'`，或 PowerShell 的 `[System.Text.Encoding]::GetEncoding(950)`。
- JDK 25 與 JDK 17 已安裝；**`mvn` 不在 PATH**，用專案內的 `mvnw.cmd`。

## 技術堆疊

Java 17 + Spring Boot 3.5.0 + Apache POI 5.3.0（**JavaFX 21.0.5 僅 `feature/gui`**），打包成 fat jar；**本分支只有 CLI 批次進入點**（JavaFX 與 GUI 在 `feature/gui`），兩分支共用同一核心服務層。

分層架構沿用 `D:\project\retained-premium-report-transformer`（`config` / `model` / `reader` / `writer` / `service` / `constant` / `exception`）與 `build.bat` / `run.bat` 慣例（本專案之 `run.bat` 已更名為 `拜託執行我.bat` 並併入編譯）；`logs/report.json` 執行報告作法比照 `D:\project\excel-report-integration-engine`。

## 溝通與文件慣例

- 回覆與文件一律使用**繁體中文**。
- 文件命名：`Health-and-Injury-Co-Insurance-System_[TYPE]_[主題].md`，TYPE 為 `REQ` / `DESIGN` / `MAPPING` / `RULE` / `TEST` / `TASK` / `GUIDE` / `LOG` / `SPEC`。
- 陳述須標示證據等級：【文件明載】/【原始碼可證】/【業務回覆】/【合理推論】/【待確認】。**不得把推論當成事實**；資訊不足時先產出待確認問題清單，不要逕行產出設計與程式碼。
- 每份文件需含：文件資訊、背景與目的、已確認事項、推論事項、待確認事項、可測試的驗收標準、版本紀錄。
